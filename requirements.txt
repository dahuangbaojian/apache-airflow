# Airflow runtime dependencies installed into the custom image.
# Keep PySpark aligned with the Spark cluster minor version.
apache-airflow-providers-apache-spark==5.6.0
playwright==1.58.0
pyspark==3.5.8
